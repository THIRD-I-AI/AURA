# ── AURA Backend Dependencies (pinned ranges) ────────────────────────
# For full packaging metadata see pyproject.toml

# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
aiofiles>=24.0,<25.0
python-multipart>=0.0.9

# Data processing
pandas>=2.2,<3.0
numpy>=1.26,<3.0
pyarrow>=16.0,<19.0

# Database drivers (core)
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
tzdata>=2024.1  # Windows: required for Alembic timezone = UTC in alembic.ini
asyncpg>=0.29,<1.0
psycopg2-binary>=2.9,<3.0
aiomysql>=0.2,<1.0
PyMySQL>=1.1,<2.0
aiosqlite>=0.20,<1.0

# Streaming
aiokafka>=0.10,<1.0
websockets>=12.0,<14.0

# AI / LLM
google-generativeai>=0.7,<1.0
groq>=0.9,<1.0

# SQL parser (used by SQLGeneratorAgent._sanitise to block destructive DDL/DML
# even when wrapped in comments or multi-statement payloads)
sqlglot>=25.0,<31.0

# Security
PyJWT>=2.8,<3.0
bcrypt>=4.0,<6.0
azure-identity>=1.16,<2.0
azure-keyvault-secrets>=4.8,<5.0

# Redis (optional — rate limiter, caching)
redis[hiredis]>=5.0,<6.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0

# Security scanning
bandit[toml]>=1.7,<2.0

# Observability
prometheus-fastapi-instrumentator>=7.0,<8.0
prometheus-client>=0.20,<1.0  # transitive, pinned for stable Counter/Histogram API
# Sentry (no-op when AURA_SENTRY_DSN unset)
sentry-sdk[fastapi]>=2.0,<3.0

# Enterprise connectors (uncomment as needed)
# pyodbc>=5.1,<6.0
# cx_Oracle>=8.3,<9.0
pymongo>=4.7,<5.0
snowflake-connector-python>=3.10,<4.0
google-cloud-bigquery>=3.24,<4.0
redshift-connector>=2.1,<3.0
databricks-sql-connector>=3.1,<4.0
clickhouse-driver>=0.2,<1.0
cassandra-driver>=3.29,<4.0
duckdb>=0.10.0,<2.0
