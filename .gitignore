# Project artifacts
graphify-out/
sprint13_demo.py
.sprint13_demo/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
.venv/
venv/
ENV/
env/

# Environment Variables
.env
*.env
!.env.example

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build outputs
dist/
build/
.vite/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Database files (runtime)
*.db
*.sqlite
*.sqlite3

# Runtime data (keep .gitkeep)
aurabackend/data/uploads/*
!aurabackend/data/uploads/.gitkeep
aurabackend/data/processed/*
!aurabackend/data/processed/.gitkeep
aurabackend/data/temp/*
!aurabackend/data/temp/.gitkeep

# Streaming pipeline checkpoints (runtime state, never commit)
aurabackend/data/checkpoints/

# Temporary files
*.tmp
*.temp

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml

# pytest
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# Backup files
*.bak
# S31b: persisted signing key (never commit)
aurabackend/data/keys/
# S31b: pre-computed demo artifacts (per-deploy, never commit)
aurabackend/data/demo_artifacts/
# Superpowers brainstorming artifacts (never commit)
.superpowers/

# ── Runtime / scratch artifacts (repo-cleanup) ──────────────────────
# Root-level streaming checkpoints (the aurabackend/ copy is ignored above;
# a second copy accumulates at the repo root from dev runs).
/data/checkpoints/
# Stray duplicate virtualenv (the real one is .venv/).
/venv/
# Service + run logs.
/logs/
aurabackend/logs/
# Loose stdout dumps (e.g. mc.out) + local scratch space.
*.out
/scratch/
